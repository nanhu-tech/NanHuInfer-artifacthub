apiVersion: v2          # 必须为 v2（Helm 3 标准）
name: nanhu-infer       # 与仓库名一致，Artifact Hub 按此匹配
version: v0.0.5          # 语义化版本（如 0.0.1，每次更新需递增）
appVersion: v0.0.1       # 对应 nanhu-infer 应用版本（与 Chart 版本可一致）
description: 面向国产推理集群的高吞吐量、低延迟推理框架 Helm Chart  # 简短描述（Artifact Hub 列表页展示）
type: application       # 固定为 application（表示应用类 Chart）
keywords:               # 关键词（方便用户搜索，至少3个）
  - 智算
  - 国产算力
  - 推理框架
  - nanhu-infer
home: https://nanhu-tech.github.io/NanHuInfer-artifacthub/  # 项目主页（GitHub 地址）
sources:                # 源代码仓库地址（Artifact Hub 会显示链接）
  - https://github.com/nanhu-tech/NanHuInfer-artifacthub
maintainers:            # 维护者信息（Artifact Hub 展示联系方式）
  - name: ycc / 3ziye
    email: support.nanhuinfer@zhejianglab.org